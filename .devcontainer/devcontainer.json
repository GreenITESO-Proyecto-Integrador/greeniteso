{
  "name": "GreenITESO Backend Dev Container",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspace/kit",
  "remoteUser": "adminuser",
  "forwardPorts": [8000],
  "mounts": [
    "source=${localWorkspaceFolder}/kit,target=/workspace/kit,type=bind",
    "source=${localWorkspaceFolder}/volume/logs,target=/workspace/kit/logs,type=bind"
  ],
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/opt/venv/bin/python",
        "python.terminal.activateEnvironment": true,
        "[python]": {
          "editor.defaultFormatter": "charliermarsh.ruff",
          "editor.formatOnSave": true,
          "editor.codeActionsOnSave": {
            "source.fixAll": "explicit",
            "source.organizeImports": "explicit"
          }
        }
      },
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
        "batisteo.vscode-django"
      ]
    }
  }
}